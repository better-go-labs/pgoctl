name: PGO Leverage Probe
# Statically evaluates whether a Go repo can benefit from PGO BEFORE running
# any full benchmark cycle. Probes the two primary PGO codegen levers:
#   1. Profile-guided INLINING  — go build -gcflags=-m=2 WITH vs WITHOUT profile
#   2. DEVIRTUALIZATION         — PGO-driven devirt markers in the same diff
# Emits: PGO-leverage = NONE | LOW | HIGH
#
# Two jobs prove correctness on known targets:
#   probe-high     : Prometheus (interface-dispatch-heavy) → expect HIGH
#   probe-none-low : nats-server (client.parse over inline budget) → expect NONE/LOW

on:
  workflow_dispatch:
  pull_request:
    paths:
      - '.github/workflows/pgo-leverage-probe.yml'
      - 'scripts/pgo-leverage/**'
      - 'action.yml'

concurrency:
  group: pgo-leverage-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write
  actions: read

jobs:
  # ── Job 1: PGO-friendly target (Prometheus) ──────────────────────────────
  probe-high:
    name: "Leverage probe — Prometheus (expect: HIGH)"
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - uses: actions/checkout@v7
        with:
          lfs: true

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Build pgoctl
        run: go build -o /tmp/pgoctl ./cmd/pgoctl

      # -------- Positive control: verify the probe mechanism works -----------
      - name: "Ctrl | Run positive-control benchmark (interface dispatch, 15s)"
        working-directory: scripts/pgo-leverage/positive-control
        run: |
          go test -bench=BenchmarkControl -benchtime=15s -cpuprofile=/tmp/ctrl.pprof .
          ls -lh /tmp/ctrl.pprof

      # go test -c compiles the test binary and emits gcflags diagnostics for
      # test packages; go build ignores *_test.go files and produces no output.
      - name: "Ctrl | gcflags -m=2 WITHOUT PGO"
        working-directory: scripts/pgo-leverage/positive-control
        run: |
          go test -c -pgo=off -gcflags="-m=2" -o /dev/null . 2>&1 | head -c 5242880 > /tmp/ctrl-no-pgo.txt || true
          echo "Inline decisions (no PGO): $(grep -cE 'can inline|inlining call' /tmp/ctrl-no-pgo.txt || echo 0)"

      - name: "Ctrl | gcflags -m=2 WITH PGO"
        working-directory: scripts/pgo-leverage/positive-control
        run: |
          go test -c -pgo=/tmp/ctrl.pprof -gcflags="-m=2" -o /dev/null . 2>&1 | head -c 5242880 > /tmp/ctrl-with-pgo.txt || true
          echo "Inline decisions (with PGO): $(grep -cE 'can inline|inlining call' /tmp/ctrl-with-pgo.txt || echo 0)"
          echo "PGO markers: $(grep -ciE 'devirtuali|pgo.driven|pgo-driven' /tmp/ctrl-with-pgo.txt || echo 0)"

      - name: "Ctrl | Report"
        env:
          TARGET_NAME: "positive-control (interface dispatch)"
          HAS_PROFILE: "true"
          GCFLAGS_NO_PGO: /tmp/ctrl-no-pgo.txt
          GCFLAGS_WITH_PGO: /tmp/ctrl-with-pgo.txt
          EXPLAIN_JSON: /dev/null
          REPORT_OUT: /tmp/ctrl-report.md
        run: python3 scripts/pgo-leverage/report.py

      - name: "Ctrl | Job summary"
        run: |
          echo "## Positive Control" >> "$GITHUB_STEP_SUMMARY"
          cat /tmp/ctrl-report.md >> "$GITHUB_STEP_SUMMARY"

      # -------- Main probe: Prometheus -------------------------------------
      - name: "Prometheus | Checkout"
        uses: actions/checkout@v7
        with:
          repository: prometheus/prometheus
          path: _prometheus
          fetch-depth: 1

      - name: "Prometheus | Cache Go modules"
        uses: actions/cache@v6
        with:
          path: ~/go/pkg/mod
          key: prom-mod-${{ hashFiles('_prometheus/go.sum') }}
          restore-keys: prom-mod-

      - name: "Prometheus | Validate + merge profile"
        run: |
          if [ -f testdata/prometheus_cpu.pprof ]; then
            /tmp/pgoctl merge --strategy weighted --out /tmp/prometheus.pgo testdata/prometheus_cpu.pprof
            echo "HAS_PROFILE=true" >> "$GITHUB_ENV"
            echo "PGO_FILE=/tmp/prometheus.pgo" >> "$GITHUB_ENV"
            echo "Profile: $(du -sh /tmp/prometheus.pgo | cut -f1)"
          else
            echo "HAS_PROFILE=false" >> "$GITHUB_ENV"
            echo "::warning::testdata/prometheus_cpu.pprof not found — probe will run baseline-only"
          fi

      - name: "Prometheus | Download deps"
        working-directory: _prometheus
        run: go mod download

      - name: "Prometheus | gcflags -m=2 WITHOUT PGO"
        working-directory: _prometheus
        run: |
          go build -pgo=off -gcflags="-m=2" ./... 2>&1 | head -c 10485760 > /tmp/prom-no-pgo.txt || true
          echo "Inline decisions (no PGO): $(grep -cE 'can inline|inlining call' /tmp/prom-no-pgo.txt || echo 0)"
          echo "PGO markers   (no PGO):   $(grep -ciE 'devirtuali|pgo.driven|pgo-driven' /tmp/prom-no-pgo.txt || echo 0)"

      - name: "Prometheus | gcflags -m=2 WITH PGO"
        working-directory: _prometheus
        run: |
          if [ "$HAS_PROFILE" = "true" ]; then
            go build -pgo="$PGO_FILE" -gcflags="-m=2" ./... 2>&1 | head -c 10485760 > /tmp/prom-with-pgo.txt || true
            echo "Inline decisions (with PGO): $(grep -cE 'can inline|inlining call' /tmp/prom-with-pgo.txt || echo 0)"
            echo "PGO markers   (with PGO):   $(grep -ciE 'devirtuali|pgo.driven|pgo-driven' /tmp/prom-with-pgo.txt || echo 0)"
          else
            echo "SKIPPED_NO_PROFILE" > /tmp/prom-with-pgo.txt
            echo "Skipped — no profile."
          fi

      - name: "Prometheus | Explain profile"
        run: |
          if [ "$HAS_PROFILE" = "true" ]; then
            /tmp/pgoctl explain --format json --top 10 /tmp/prometheus.pgo \
              > /tmp/prom-explain.json 2>/dev/null || echo "{}" > /tmp/prom-explain.json
          else
            echo "{}" > /tmp/prom-explain.json
          fi

      - name: "Prometheus | Generate leverage report"
        env:
          TARGET_NAME: "prometheus/prometheus"
          GCFLAGS_NO_PGO: /tmp/prom-no-pgo.txt
          GCFLAGS_WITH_PGO: /tmp/prom-with-pgo.txt
          EXPLAIN_JSON: /tmp/prom-explain.json
          REPORT_OUT: /tmp/prom-leverage-report.md
        run: |
          export HAS_PROFILE="$HAS_PROFILE"
          python3 scripts/pgo-leverage/report.py

      - name: "Prometheus | Job summary"
        run: |
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "---" >> "$GITHUB_STEP_SUMMARY"
          echo "## Prometheus Leverage Probe" >> "$GITHUB_STEP_SUMMARY"
          cat /tmp/prom-leverage-report.md >> "$GITHUB_STEP_SUMMARY"

      - name: "Prometheus | Sticky PR comment"
        if: github.event_name == 'pull_request'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: pgo-leverage-prometheus
          path: /tmp/prom-leverage-report.md

      - name: Upload probe artifacts (Prometheus)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pgo-leverage-probe-prometheus
          path: |
            /tmp/ctrl-report.md
            /tmp/prom-no-pgo.txt
            /tmp/prom-with-pgo.txt
            /tmp/prom-explain.json
            /tmp/prom-leverage-report.md
          if-no-files-found: warn

  # ── Job 2: Known-flat target (nats-server: client.parse over inline budget) ──
  probe-none-low:
    name: "Leverage probe — nats-server (expect: NONE/LOW)"
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-go@v7
        with:
          go-version-file: go.mod
          cache: true

      - name: Build pgoctl
        run: go build -o /tmp/pgoctl ./cmd/pgoctl

      - name: "nats-server | Checkout (public)"
        uses: actions/checkout@v7
        with:
          repository: nats-io/nats-server
          path: _nats-server
          fetch-depth: 1

      # Try to fetch a profile artifact from the better-go-labs fork;
      # gracefully skip if unavailable (cross-repo artifact access may be restricted).
      # Use ?per_page=100 (no --paginate) so the jq filter runs over the full
      # result set; --paginate applies jq per page so `last` would be wrong.
      - name: "nats-server | Fetch PGO profile artifact (if available)"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set +e
          RUN_ID=$(gh api "/repos/better-go-labs/nats-server/actions/artifacts?per_page=100" \
            --jq '[.artifacts[] | select(.name=="default-pgo" and .expired==false)] | sort_by(.created_at) | last | .workflow_run.id' \
            2>/dev/null || echo "")
          if [ -n "$RUN_ID" ] && [ "$RUN_ID" != "null" ] && [ "$RUN_ID" != "" ]; then
            gh run download "$RUN_ID" \
              --repo "better-go-labs/nats-server" \
              --name default-pgo \
              --dir /tmp/nats-profile 2>/dev/null || true
          fi
          PGO_FILE=$(find /tmp/nats-profile -type f 2>/dev/null | head -1)
          if [ -n "$PGO_FILE" ]; then
            echo "HAS_PROFILE=true" >> "$GITHUB_ENV"
            echo "NATS_PGO_FILE=$PGO_FILE" >> "$GITHUB_ENV"
            echo "Profile: $PGO_FILE ($(du -sh "$PGO_FILE" | cut -f1))"
          else
            echo "HAS_PROFILE=false" >> "$GITHUB_ENV"
            echo "No nats-server profile artifact available — running baseline-only probe."
            echo "Note: NONE verdict requires a profile. Baseline analysis still reveals inline budget violations."
          fi
          set -e

      - name: "nats-server | gcflags -m=2 WITHOUT PGO"
        working-directory: _nats-server
        run: |
          go build -pgo=off -gcflags="-m=2" . 2>&1 | head -c 10485760 > /tmp/nats-no-pgo.txt || true
          echo "Inline decisions (no PGO): $(grep -cE 'can inline|inlining call' /tmp/nats-no-pgo.txt || echo 0)"
          echo "PGO markers   (no PGO):   $(grep -ciE 'devirtuali|pgo.driven|pgo-driven' /tmp/nats-no-pgo.txt || echo 0)"
          # Surface the client.parse inline-budget finding (the root cause from BP-23)
          grep -iE 'client.*parse.*budget|parse.*too large' /tmp/nats-no-pgo.txt || \
            echo "(client.parse inline-budget violation not surfaced at -m=2; use -m=3 for verbose)"

      - name: "nats-server | gcflags -m=2 WITH PGO"
        working-directory: _nats-server
        run: |
          if [ "$HAS_PROFILE" = "true" ]; then
            go build -pgo="$NATS_PGO_FILE" -gcflags="-m=2" . 2>&1 | head -c 10485760 > /tmp/nats-with-pgo.txt || true
            echo "Inline decisions (with PGO): $(grep -cE 'can inline|inlining call' /tmp/nats-with-pgo.txt || echo 0)"
            echo "PGO markers   (with PGO):   $(grep -ciE 'devirtuali|pgo.driven|pgo-driven' /tmp/nats-with-pgo.txt || echo 0)"
          else
            echo "SKIPPED_NO_PROFILE" > /tmp/nats-with-pgo.txt
            echo "Skipped — no profile available."
          fi

      - name: "nats-server | Explain profile"
        run: |
          if [ "$HAS_PROFILE" = "true" ]; then
            /tmp/pgoctl explain --format json --top 10 "$NATS_PGO_FILE" \
              > /tmp/nats-explain.json 2>/dev/null || echo "{}" > /tmp/nats-explain.json
          else
            echo "{}" > /tmp/nats-explain.json
          fi

      - name: "nats-server | Generate leverage report"
        env:
          TARGET_NAME: "nats-io/nats-server"
          GCFLAGS_NO_PGO: /tmp/nats-no-pgo.txt
          GCFLAGS_WITH_PGO: /tmp/nats-with-pgo.txt
          EXPLAIN_JSON: /tmp/nats-explain.json
          REPORT_OUT: /tmp/nats-leverage-report.md
        run: |
          export HAS_PROFILE="$HAS_PROFILE"
          python3 scripts/pgo-leverage/report.py

      - name: "nats-server | Job summary"
        run: |
          echo "## nats-server Leverage Probe" >> "$GITHUB_STEP_SUMMARY"
          cat /tmp/nats-leverage-report.md >> "$GITHUB_STEP_SUMMARY"

      - name: "nats-server | Sticky PR comment"
        if: github.event_name == 'pull_request'
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          header: pgo-leverage-nats
          path: /tmp/nats-leverage-report.md

      - name: Upload probe artifacts (nats-server)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: pgo-leverage-probe-nats-server
          path: |
            /tmp/nats-no-pgo.txt
            /tmp/nats-with-pgo.txt
            /tmp/nats-explain.json
            /tmp/nats-leverage-report.md
          if-no-files-found: warn
